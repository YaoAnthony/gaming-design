# map

`world.json` 是地图的唯一数据源：房间字符画 + 布局 + 房间名。

- `.` 空气  `#` 泥土  `R` 岩石  `B` 脆岩  `S` 沙土  `X` 尖刺
- `P` 出生点  `M` 怪物  `G` 终点
- 每个房间 `roomH` 行，每行 `roomW` 个字符；`layout[ry][rx]` 决定房间怎么拼，第一行是最上面一排
- 所有泥土 / 脆岩 / 沙土必须（经由相邻格子）连到岩石，否则第一次爆炸就会掉落；编辑器会用红框标出

用编辑器改完点「导出 world.json」，把文件覆盖到这里即可。
