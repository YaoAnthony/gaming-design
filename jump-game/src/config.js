// 全局参数，方便调手感
const GAME_CONFIG = {
  width: 800,
  height: 600,
  gravity: 1200,
  playerSpeed: 260,
  jumpVelocity: -560,
  doubleJump: true,
  coyoteTimeMs: 100,     // 离开平台后仍可起跳的宽限时间
  jumpBufferMs: 120,     // 落地前按跳的缓冲时间
};
